name: build
description: builds iscenv binaries

runs:
  using: composite
  steps:
    - uses: actions/setup-go@v3
      with:
        go-version: 1.19
    - run: make clean build
      shell: bash
    - uses: actions/upload-artifact@v3
      with:
        name: build-artifact
        path: pkg/iscenv
